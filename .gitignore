# Compiled binaries (Makefile builds into bin/)
/bin/
*.o
*.out

# Python virtualenv (used only for PDF text extraction)
.venv_pdf/

# SLURM / run logs
*.log

# Benchmark output data (regenerable, can be large)
results*/
*.dump

# Reference PDFs (assignment spec & course notes)
*.pdf

# Course example code (professor-provided reference, not part of the deliverable)
Code/
